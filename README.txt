Windows / Python 3.12.10 / FFmpeg. Extract, open PowerShell in qubi_full_flow, run .\BUILD_WITH_CHATTERBOX.ps1 to install and hear chatterbox_cache/takes/greeting.wav. When satisfied, run .\BUILD_WITH_CHATTERBOX.ps1 -Build to automatically produce all 32 voice clips and the full MP4. Uses a dedicated .venv312 virtual environment and checks narration timing before export. First run downloads model files. No API credit needed.
